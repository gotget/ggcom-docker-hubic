#!/usr/bin/env bash
: <<'!COMMENT'

GGCOM - Docker - hubiC v201508061426
Louis T. Getterman IV (@LTGIV)
www.GotGetLLC.com | www.opensour.cc/ggcom/docker/hubic

!COMMENT

# Stop and remove
docker stop $(docker ps --all --quiet --no-trunc --filter 'name=hubic');
docker rm $(docker ps --all --quiet --no-trunc --filter 'name=hubic')
